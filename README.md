# BusyPlugin
A simple Bukkit plugin to let players toggle /busy status and block /msg when busy.
# BusyPlugin

A Bukkit plugin to let players toggle busy status and block private messages using `/msg`, `/tell`, or `/w`.

## Commands

- `/busy`: Toggles busy status.

## Features

- Prevents messages to busy players.

